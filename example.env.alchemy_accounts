# Add your Alchemy accounts in format - 'host_name email password'
# The script will not work if you password contains spaces
# The host_name should be the same with host_name in .env.servers
# Example
contabo_1 root myP@ssw0rd