# Add your Alchemy accounts in format - 'host_name email password'
# The script will not work if your password/host_name contains spaces
# The host_name should be the same with host_name in .env.servers
# Example
contabo_1 root@email.com myP@ssw0rd
contabo_2 root@email.com myP@ssw0rd